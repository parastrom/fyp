
def schema_linking(query, db, extra_values):
    lst_match_values = []

    # for col, cell_index in zip(db.columns, db.column):

if __name__ == "__main__":
    import argparse
    import datasets

    arg_parser = argparse.ArgumentParser("Schema Linking")




